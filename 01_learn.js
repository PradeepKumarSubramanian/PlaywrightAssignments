const browserName = "Chrome";

function getBrowserVersion(browserName){
    if(browserName == "Chrome"){
        var browseVersion = "120.0.6099.199" 
    }
    console.log(browseVersion);   
}

getBrowserVersion(browserName);